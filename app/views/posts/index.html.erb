<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<% @posts.each do |post| %>
			<div class="well">
				<h4><%=post.title %></h4>
				<p><%= post.body %></p>
				<% if signed_in? %>
					<p>Posted by: <%= post.user.name %></p>
				<% end %>
			</div>
		<% end %>
	</div>
</div>